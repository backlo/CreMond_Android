<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lenovo.cremond_android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="쏙쏙러닝"
        android:roundIcon="@mipmap/icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".LoadingActivity"/>
        <activity android:name=".QRActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Animal.AnimalActivity" />
        <activity android:name=".Body.BodyActivity" />
        <activity android:name=".Color.ColorActivity" />
        <activity android:name=".Job.JobActivity" />
        <activity android:name=".Number.NumberActivity" />
        <activity android:name=".Objects.ObjectsActivity" />
        <activity android:name=".Plant.PlantActivity" />
        <activity android:name=".Vehicle.VehicleActivity" />
    </application>

</manifest>